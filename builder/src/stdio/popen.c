#include <stdio.h>
#include <stdlib.h>

FILE *popen(const char *command, const char *mode)
{
  // TODO - EMULATE SHELL FOR DIFFERENT HOSTS:
  //     - web js dom int
  //     - native, host plugin that uses similar mechanism to native
    (void)command; (void)mode;
    return NULL;
}

