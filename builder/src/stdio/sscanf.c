/**
 * sscanf.c
 *
 * Reads from a string with the given format.
 * Minimal stub returning 0.
 */

#include <stdio.h>
#include <stdarg.h>

int sscanf(const char *str, const char *format, ...)
{
    (void)str;
    (void)format;
    return 0;
}

