/**
 * scanf.c
 *
 * Minimal stub or partial logic that calls vscanf().
 * We'll do a stub returning 0 or you can do real parsing.
 */

#include <stdio.h>
#include <stdarg.h>

int scanf(const char *format, ...)
{
  // TODO - implement
    // For real usage, you'd parse the input from stdin. We'll do a stub:
    (void)format;
    return 0;
}

