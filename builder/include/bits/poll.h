#ifndef _BITS_POLL_H
#define _BITS_POLL_H

/*
 * Minimal WASM version of bits/poll.h.
 * In musl, this file might contain arch-specific offsets or flags.
 * Here, we don't define anything extra for WebAssembly.
 *
 * If your code needs architecture-specific constants for poll,
 * you can add them below.
 */

#endif /* _BITS_POLL_H */

